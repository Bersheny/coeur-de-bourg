<form method="post" action="{{ path('app_partners_delete', {'id': cdb_partners.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce partenaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cdb_partners.id) }}">
    <button class="btn btn-danger btn-sm mt-2" style="font-family: Amatic SC; font-size: 22px; font-weight: 700;">Supprimer</button>
</form>
