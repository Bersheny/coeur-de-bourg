{# {% extends 'base.html.twig' %}

{% block title %}CdbUsers{% endblock %}

{% block body %}
    <h1>CdbUsers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cdb_user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cdb_user.email }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ cdb_user.role ? cdb_user.role|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ cdb_user.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ cdb_user.lastName }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ cdb_user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_users_index') }}">back to list</a>

    <a href="{{ path('app_users_edit', {'id': cdb_user.id}) }}">edit</a>

    {{ include('users/_delete_form.html.twig') }}
{% endblock %} #}
