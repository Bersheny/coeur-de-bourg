{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary mt-3">{{ button_label|default('Créer') }}</button>
<a class="btn btn-secondary mt-3" href="{{ path('app_news_index') }}">Revenir à la liste</a>
{{ form_end(form) }}

