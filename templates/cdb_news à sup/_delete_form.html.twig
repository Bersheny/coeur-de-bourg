<form method="post" action="{{ path('app_cdb_news_delete', {'id': cdb_news.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cdb_news.id) }}">
    <button class="btn">Delete</button>
</form>
